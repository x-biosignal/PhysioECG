# Find a wave peak (P or T) in a time window relative to R-peak

Find a wave peak (P or T) in a time window relative to R-peak

## Usage

``` r
.find_wave_peak(
  sig,
  r_pos,
  win_start_samples,
  win_end_samples,
  n_time,
  find_max = TRUE
)
```
