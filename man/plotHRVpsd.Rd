% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ecg-plot.R
\name{plotHRVpsd}
\alias{plotHRVpsd}
\title{Plot HRV Power Spectral Density with Frequency Bands}
\usage{
plotHRVpsd(
  rr,
  method = c("welch", "ar", "lomb"),
  detrend = FALSE,
  channel = NULL,
  vlf_band = c(0.003, 0.04),
  lf_band = c(0.04, 0.15),
  hf_band = c(0.15, 0.4)
)
}
\arguments{
\item{rr}{An rr data.frame or an object from \code{\link{ecgProcess}}.}

\item{method}{Spectral method ("welch" default, "ar" or "lomb"), matching
\code{\link{ecgHRVfreq}}.}

\item{detrend}{Smoothness-priors detrending, matching \code{\link{ecgHRVfreq}}.}

\item{channel}{Channel to plot (default: first).}

\item{vlf_band, lf_band, hf_band}{Band edges (Hz).}
}
\value{
A ggplot object with a \code{"band_power"} attribute.
}
\description{
Plots the RR-interval power spectrum with the VLF, LF and HF bands shaded and
their integrated powers annotated. The PSD and band integration reuse the same
internals as \code{\link{ecgHRVfreq}}, so the shaded band areas equal the
\code{ecgHRVfreq} band powers. The band powers are attached as the
\code{"band_power"} attribute.
}
\examples{
pe <- make_ecg(40000, sr = 500)
rr <- ecgRRintervals(pe, ecgDetectRpeaks(pe))
plotHRVpsd(rr)
}
\references{
Task Force (1996). Heart rate variability. \emph{Circulation},
93(5), 1043-1065.
}
\seealso{
\code{\link{ecgHRVfreq}}
}
