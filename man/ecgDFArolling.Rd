% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ecg-hrv-nonlinear.R
\name{ecgDFArolling}
\alias{ecgDFArolling}
\title{Rolling (time-resolved) DFA alpha1}
\usage{
ecgDFArolling(rr, window_beats = 300, step_beats = 30, short_range = c(4, 16))
}
\arguments{
\item{rr}{A data.frame with columns \code{channel} and \code{rr_ms}.}

\item{window_beats}{Window length in beats (default 300).}

\item{step_beats}{Step between successive windows in beats (default 30).}

\item{short_range}{Scale range (in beats) for alpha1 (default c(4, 16)).}
}
\value{
A data.frame with one row per window and columns \code{channel},
\code{beat_start}, \code{beat_end}, \code{beat_center} and \code{alpha1}.
}
\description{
Computes the short-range detrended-fluctuation scaling exponent (alpha1)
over a sliding window of beats, yielding a time-resolved alpha1 trajectory.
This is the basis of the DFA-a1 exercise-intensity thresholds of Gronwald &
Rogers (2020).
}
\examples{
set.seed(1)
rr <- data.frame(channel = 1L, rr_ms = 800 + cumsum(rnorm(1000, sd = 2)))
traj <- ecgDFArolling(rr, window_beats = 200, step_beats = 50)
}
\references{
Peng, C.K. et al. (1995). Quantification of scaling exponents.
\emph{Chaos}, 5(1), 82-87. Gronwald, T. & Rogers, B. (2020). Fractal
correlation properties of heart rate variability as a biomarker.
\emph{Frontiers in Physiology}, 11, 550572.
}
\seealso{
\code{\link{ecgDFA}}, \code{\link{dfaThresholdCrossings}}
}
