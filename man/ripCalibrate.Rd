% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resp.R
\name{ripCalibrate}
\alias{ripCalibrate}
\title{Calibrate two-belt respiratory inductance plethysmography}
\usage{
ripCalibrate(
  rc,
  ab,
  reference = NULL,
  sr = NULL,
  method = c("qdc", "lsq"),
  band = c(0.1, 0.6),
  window = NULL,
  fit_intercept = FALSE,
  known_volume = NULL
)
}
\arguments{
\item{rc}{Ribcage signal as a numeric vector, or a \code{PhysioExperiment}.}

\item{ab}{Abdominal numeric vector, or a channel index/label when \code{rc} is a
\code{PhysioExperiment}.}

\item{reference}{Optional reference volume signal, required for \code{"lsq"}.}

\item{sr}{Sampling rate in Hz for numeric signals. A \code{PhysioExperiment}
supplies its own sampling rate.}

\item{method}{Calibration method, \code{"qdc"} or \code{"lsq"}.}

\item{band}{Two-element respiratory band in Hz.}

\item{window}{Optional inclusive calibration sample range \code{c(lo, hi)}.}

\item{fit_intercept}{Logical; include an intercept in least squares.}

\item{known_volume}{Optional known inspiratory volume used for absolute
scaling.}
}
\value{
A \code{rip_calibration} object containing coefficients, calibration
diagnostics, and the calibrated volume-proportional signal.
}
\description{
Fits the Konno-Mead two-degree-of-freedom volume model from ribcage and
abdominal belt signals. Least-squares calibration uses a reference volume
trace; qualitative diagnostic calibration (QDC) equalizes the variability
of the two belt contributions.
}
\examples{
sr <- 10
time <- (0:599) / sr
rc <- 2 * sin(2 * pi * 0.25 * time)
ab <- sin(2 * pi * 0.25 * time)
ripCalibrate(rc, ab, sr = sr)
}
\references{
Konno K, Mead J (1967). Measurement of the separate volume changes of rib
cage and abdomen during breathing. \emph{Journal of Applied Physiology},
22:407-422.

Sackner MA, Watson H, Belsito AS, et al. (1989). Calibration of respiratory
inductive plethysmograph during natural breathing. \emph{Journal of Applied
Physiology}, 66:410-420. \doi{10.1152/jappl.1989.66.1.410}
}
